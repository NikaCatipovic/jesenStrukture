#include <stdio.h>
#include <stdlib.h>

typedef struct node {  
	int data;			
	struct node* next;	//pokazivac na sljedeci element
}Node;

int main() {

	Node* head = NULL; //head element postoji, ali ne pokazuje na ista

	return 0;
}